# Інструкція з розгортання HR застосунку на Render

## Передумови
- ✅ Код зібрано локально (`npm run build` в `client/` та `server/`)
- ✅ Код завантажено до GitHub: https://github.com/IluhanCoder/hr_app.git
- ✅ Обліковий запис на [render.com](https://render.com)
- ✅ MongoDB Atlas база даних (або інша MongoDB база даних з публічним доступом)

---

## Крок 1: Створити Web Service для Backend (Node.js API)

### 1.1. Зайдіть на Render Dashboard
- Відкрийте https://dashboard.render.com/
- Натисніть кнопку **"New +"** → **"Web Service"**

### 1.2. Підключіть GitHub репозиторій
- Виберіть **"Connect a repository"**
- Знайдіть та виберіть репозиторій: `IluhanCoder/hr_app`
- Натисніть **"Connect"**

### 1.3. Налаштуйте Backend Service

Введіть наступні параметри:

| Поле | Значення |
|------|----------|
| **Name** | `hr-app-backend` (або будь-яке ім'я) |
| **Region** | Виберіть найближчий регіон (напр. Frankfurt) |
| **Branch** | `main` |
| **Root Directory** | `server` |
| **Runtime** | `Node` |
| **Build Command** | `npm install && npm run build` |
| **Start Command** | `node dist/server/src/app.js` |
| **Instance Type** | `Free` (для початку) |

### 1.4. Додайте змінні середовища (Environment Variables)

Натисніть **"Add Environment Variable"** та додайте:

| Ключ | Значення | Опис |
|------|----------|------|
| `MONGODB_URI` | `mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority` | Ваш MongoDB Atlas connection string |
| `JWT_SECRET` | `your-super-secret-jwt-key-change-this` | Секретний ключ для JWT токенів (згенеруйте складний!) |
| `PORT` | `5000` | Порт для сервера (Render автоматично визначає) |
| `CLIENT_URL` | `https://hr-app-frontend.onrender.com` | URL фронтенду (додасте після створення фронтенду) |
| `NODE_ENV` | `production` | Середовище |

**⚠️ Важливо:** 
- Змініть `JWT_SECRET` на складний випадковий рядок!
- Вставте реальний MongoDB URI з Atlas

### 1.5. Створіть сервіс
- Натисніть **"Create Web Service"**
- Render почне автоматичний деплой (це займе 2-5 хвилин)

### 1.6. Збережіть URL Backend
Після успішного розгортання скопіюйте URL (наприклад: `https://hr-app-backend.onrender.com`)

---

## Крок 2: Створити Static Site для Frontend (React)

### 2.1. Повторно натисніть "New +"
- На Dashboard натисніть **"New +"** → **"Static Site"**

### 2.2. Підключіть той самий репозиторій
- Виберіть репозиторій: `IluhanCoder/hr_app`
- Натисніть **"Connect"**

### 2.3. Налаштуйте Frontend Static Site

| Поле | Значення |
|------|----------|
| **Name** | `hr-app-frontend` |
| **Branch** | `main` |
| **Root Directory** | `client` |
| **Build Command** | `npm install && npm run build` |
| **Publish Directory** | `build` |

### 2.4. Додайте змінну середовища для API URL

**Дуже важливо!** Додайте змінну, щоб фронтенд знав адресу вашого бекенду:

| Ключ | Значення |
|------|----------|
| `REACT_APP_API_URL` | `https://hr-app-backend.onrender.com` |

**⚠️ Замініть** `https://hr-app-backend.onrender.com` на реальний URL з **Кроку 1.6**

### 2.5. Створіть Static Site
- Натисніть **"Create Static Site"**
- Render побудує та розгорне фронтенд (2-5 хвилин)

### 2.6. Збережіть URL Frontend
Скопіюйте URL фронтенду (наприклад: `https://hr-app-frontend.onrender.com`)

---

## Крок 3: Оновити CLIENT_URL в Backend

### 3.1. Поверніться до Backend Web Service
- Відкрийте Dashboard → вибрати `hr-app-backend`
- Перейдіть на вкладку **"Environment"**

### 3.2. Оновіть змінну CLIENT_URL
- Знайдіть змінну `CLIENT_URL`
- Змініть значення на реальний URL фронтенду з **Кроку 2.6**
- Натисніть **"Save Changes"**

### 3.3. Render автоматично перезапустить backend

---

## Крок 4: Перевірка розгортання

### 4.1. Перевірте Backend API
Відкрийте у браузері (або через `curl`):
```
https://hr-app-backend.onrender.com/
```

Повинно повернути базову відповідь або документацію API.

### 4.2. Перевірте Frontend
Відкрийте у браузері:
```
https://hr-app-frontend.onrender.com
```

Повинна відкритися сторінка логіну HR застосунку.

### 4.3. Тест авторизації
- Спробуйте зареєструватися або увійти
- Перевірте, чи працюють запити до API (відкрийте DevTools → Network)

---

## Крок 5: Налаштування MongoDB Atlas (якщо ще не зроблено)

### 5.1. Дозвольте доступ з будь-якого IP
- Зайдіть на [MongoDB Atlas Dashboard](https://cloud.mongodb.com/)
- Виберіть ваш кластер → **Network Access**
- Додайте IP адресу: `0.0.0.0/0` (дозволити всі IP для Render)
  - ⚠️ **Увага:** Це дозволяє доступ з будь-якого IP. Для продакшн середовища краще обмежити.

### 5.2. Створіть Database User
- **Database Access** → **Add New Database User**
- Встановіть Username та Password
- Надайте права: **Read and write to any database**
- Використайте ці credentials у `MONGODB_URI`

---

## Крок 6: Налаштування CORS та варіанти для Production

### 6.1. Перевірте CORS налаштування в `server/src/app.ts`
Переконайтеся, що `CLIENT_URL` правильно використовується:
```typescript
app.use(cors({
  origin: process.env.CLIENT_URL || 'http://localhost:3000',
  credentials: true
}));
```

---

## Troubleshooting (Можливі проблеми)

### ❌ Backend не стартує
1. Перевірте Logs на Render Dashboard (кнопка **"Logs"**)
2. Переконайтеся, що `MONGODB_URI` правильний
3. Перевірте, що MongoDB Atlas дозволяє доступ з IP `0.0.0.0/0`

### ❌ Frontend не може з'єднатися з Backend
1. Перевірте `REACT_APP_API_URL` в Environment Variables фронтенду
2. Переконайтеся, що `CLIENT_URL` в бекенді вказує на правильний URL фронтенду
3. Відкрийте DevTools → Console і перевірте помилки CORS

### ❌ Build fails
1. Перевірте Logs на Render
2. Переконайтеся, що локальний build працює (`npm run build`)
3. Якщо помилка TypeScript, виправте код та push знову до GitHub

### ❌ "Cannot find module" помилка
- Переконайтеся, що всі залежності в `package.json` (не в `devDependencies` для production залежностей)
- У `server/package.json` перенесіть необхідні типи (`@types/*`) в `dependencies` якщо потрібно під час runtime

---

## Додаткові рекомендації

### 🔒 Безпека
- Змініть `JWT_SECRET` на складний випадковий рядок
- Не використовуйте `0.0.0.0/0` для MongoDB у продакшн (обмежте IP Render серверів)
- Додайте rate limiting для API endpoints

### 📊 Моніторинг
- Render надає базові метрики на Dashboard
- Налаштуйте Alerts для crash-ів (Dashboard → Settings → Notifications)

### 🚀 Оновлення коду
Кожен раз, коли ви робите `git push` до `main` гілки, Render **автоматично** переразгорне сервіси!

---

## Підсумок

Ваш HR застосунок тепер розгорнуто на Render:

- **Backend API**: `https://hr-app-backend.onrender.com`
- **Frontend**: `https://hr-app-frontend.onrender.com`

✅ Автоматичні деплої при push до GitHub  
✅ HTTPS з коробки  
✅ Free tier для тестування  

**Вітаю, розгортання завершено!** 🎉
