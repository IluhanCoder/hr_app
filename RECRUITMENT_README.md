# Recruitment System (UC-202) - Система Рекрутингу

## Огляд
Повнофункціональна система управління процесом найму з воронкою кандидатів, плануванням інтерв'ю, збором фідбеку та генерацією офферів.

## Реалізовані вимоги

### FR-202a: Обов'язкове планування інтерв'ю
- ✅ Система **обов'язково** вимагає запланувати інтерв'ю при переміщенні на етапи:
  - Технічне інтерв'ю
  - HR інтерв'ю
  - Фінальне інтерв'ю
- ✅ Інтеграція з календарем (дата/час, локація, посилання на зустріч)
- ✅ Вибір інтерв'юерів з користувачів системи

### FR-202b: Збір фідбеку від інтерв'юерів
- ✅ Кожен інтерв'юер може залишити фідбек після інтерв'ю
- ✅ Фідбек включає:
  - Рейтинг (1-5 зірок)
  - Рекомендацію (Однозначно так / Так / Можливо / Ні / Однозначно ні)
  - Коментар
- ✅ Автоматична зміна статусу інтерв'ю на "Завершено" після фідбеку від всіх інтерв'юерів
- ✅ Заборона дублювання фідбеку від одного інтерв'юера

### FR-202c: Генерація оффера після успішного проходження
- ✅ Оффер можна згенерувати тільки після завершених інтерв'ю
- ✅ Перевірка середнього рейтингу (має бути >= 3.5)
- ✅ Оффер включає:
  - Посаду
  - Зарплату та валюту
  - Дату початку роботи
  - Список переваг (benefits)
- ✅ Можливість надіслати оффер кандидату
- ✅ Відстеження статусу оффера (чернетка/надіслано/прийнято/відхилено)

## Архітектура

### Backend
```
server/src/recruitment/
├── recruitment.model.ts      # Mongoose схема кандидата
├── recruitment.controller.ts # 10 методів (CRUD + бізнес-логіка)
└── recruitment.routes.ts     # REST API з авторизацією
```

**API Endpoints:**
- `GET /api/recruitment` - Список кандидатів (з фільтрами)
- `GET /api/recruitment/stats` - Статистика по воронці
- `GET /api/recruitment/:id` - Деталі кандидата
- `POST /api/recruitment` - Створити кандидата
- `POST /api/recruitment/:id/move` - Перемістити по воронці (FR-202a)
- `POST /api/recruitment/:candidateId/interviews/:interviewId/feedback` - Додати фідбек (FR-202b)
- `POST /api/recruitment/:id/offer/generate` - Згенерувати оффер (FR-202c)
- `POST /api/recruitment/:id/offer/send` - Надіслати оффер
- `POST /api/recruitment/:id/offer/respond` - Відповісти на оффер
- `POST /api/recruitment/:id/reject` - Відхилити кандидата

### Frontend
```
client/src/
├── types/recruitment.types.ts        # TypeScript типи
└── pages/
    ├── RecruitmentPipeline.tsx       # Kanban воронка найму
    ├── CandidateDetails.tsx          # Деталі + управління (1000+ рядків)
    └── NewCandidate.tsx              # Форма додавання
```

**Сторінки:**
1. **RecruitmentPipeline** (`/recruitment`)
   - Kanban дошка з колонками по етапах
   - Статистика (всього/активні/найнято/відхилено)
   - Фільтри (етап, статус)
   - Пошук по імені/email/посаді

2. **CandidateDetails** (`/recruitment/:id`)
   - Контактна інформація + статус
   - Список інтерв'ю з фідбеком
   - Модальне вікно переміщення (з обов'язковим плануванням інтерв'ю)
   - Модальне вікно додавання фідбеку
   - Модальне вікно генерації оффера
   - Історія переміщень

3. **NewCandidate** (`/recruitment/new`)
   - Форма створення кандидата
   - Валідація полів
   - Вибір рекрутера

## Етапи воронки (Recruitment Stages)

1. **Application** (Заявка) - початковий етап
2. **Screening** (Скринінг) - первинний відбір
3. **Technical Interview** (Технічне інтерв'ю) ⚠️ потрібне інтерв'ю
4. **HR Interview** (HR інтерв'ю) ⚠️ потрібне інтерв'ю
5. **Final Interview** (Фінальне інтерв'ю) ⚠️ потрібне інтерв'ю
6. **Offer** (Оффер) - оффер згенеровано
7. **Hired** (Найнято) - фінальний успішний етап
8. **Rejected** (Відхилено) - кандидат відхилений

## Контроль доступу

**Можуть створювати кандидатів:**
- HR Manager
- Admin
- Recruiter

**Можуть переміщати по воронці:**
- HR Manager
- Admin
- Line Manager (для інтерв'ю)
- Recruiter

**Можуть залишати фідбек:**
- Будь-який користувач, призначений інтерв'юером

**Можуть генерувати оффери:**
- HR Manager
- Admin
- Recruiter

## Ключові особливості

### Валідація
- Обов'язкове інтерв'ю для певних етапів (FR-202a)
- Перевірка середнього рейтингу перед генерацією оффера (FR-202c)
- Заборона повторного фідбеку від одного інтерв'юера (FR-202b)
- Валідація email кандидата (унікальність)

### UX
- Модальні вікна для всіх основних дій
- Реал-тайм оновлення після змін
- Візуальна індикація статусів (кольорові бейджі)
- Рейтинг зірочками
- Responsive design

### Безпека
- Всі маршрути захищені authMiddleware
- Рольова авторизація через requireRole
- Валідація на рівні backend та frontend

## Використання

### 1. Додати нового кандидата
```
Dashboard → Рекрутинг → "+ Додати кандидата"
Заповнити форму → Додати кандидата
```

### 2. Перемістити по воронці
```
Клік на кандидата → "Перемістити далі"
Обрати етап → Якщо потрібно інтерв'ю - заповнити форму
Вказати дату, інтерв'юерів, локацію → Перемістити
```

### 3. Залишити фідбек
```
Клік на кандидата → Знайти своє інтерв'ю
"Залишити фідбек" → Вказати рейтинг, рекомендацію, коментар
Відправити фідбек
```

### 4. Згенерувати оффер
```
Після завершених інтерв'ю → "Згенерувати оффер"
Заповнити деталі (посада, зарплата, дата початку, переваги)
Згенерувати оффер → Надіслати оффер
```

## Типи даних

### Candidate (Кандидат)
```typescript
{
  firstName: string
  lastName: string
  email: string
  phone?: string
  resumeUrl?: string
  linkedinUrl?: string
  position: string
  department: string
  currentStage: RecruitmentStage
  status: CandidateStatus
  interviews: Interview[]
  stageHistory: StageHistory[]
  offer?: Offer
  source?: string
  assignedTo?: User
}
```

### Interview (Інтерв'ю)
```typescript
{
  scheduledAt: Date
  interviewers: User[]
  location?: string
  meetingLink?: string
  notes?: string
  feedback: InterviewFeedback[]
  status: "scheduled" | "completed" | "cancelled"
}
```

### InterviewFeedback (Фідбек)
```typescript
{
  from: User
  rating: number (1-5)
  comment: string
  recommendation: "strong_yes" | "yes" | "maybe" | "no" | "strong_no"
  createdAt: Date
}
```

### Offer (Оффер)
```typescript
{
  position: string
  salary: number
  currency: string
  startDate: Date
  benefits?: string[]
  generatedBy: User
  status: "draft" | "sent" | "accepted" | "rejected" | "expired"
}
```

## Інтеграція з Dashboard

Картка "Рекрутинг" 🎯 доступна для:
- HR Manager
- Admin
- Recruiter
- Line Manager

## Технології

**Backend:**
- Node.js + Express + TypeScript
- MongoDB + Mongoose
- JWT Authentication

**Frontend:**
- React + TypeScript
- MobX (state management)
- Tailwind CSS
- React Router

## Статус реалізації

✅ **Повністю реалізовано:**
- FR-202a: Обов'язкове планування інтерв'ю
- FR-202b: Збір фідбеку від інтерв'юерів
- FR-202c: Генерація оффера після успішного проходження
- UC-202: Проведення по воронці

**Всі вимоги виконані та готові до production use!**
